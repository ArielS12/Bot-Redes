@* Modo publico: ya no se restaura JWT desde localStorage.
   Para reactivar, descomentar el bloque inferior.

@inject ClientAuthSession Auth

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Auth.InitializeFromBrowserAsync();
        }
    }
}
*@
